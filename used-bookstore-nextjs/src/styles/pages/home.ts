import { styled } from "..";

export const CentralizeContainer = styled("main", {
  display: "flex",
  zIndex: 20,
  flexDirection: "column",
  width: "100%",
  alignItems: "center",
});

export const Container = styled("div", {
  display: "flex",
  zIndex: 20,
  flexDirection: "column",
  width: "1180px",
});
